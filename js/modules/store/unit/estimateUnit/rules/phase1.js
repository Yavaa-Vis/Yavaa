"use strict";
/**
 * returns a list of phase 1 transformation rules for estimateUnit
 */
define( [
          'store/unit/estimateUnit/rules/collectSummands'
], function(){

  var rules = Array.prototype.slice.call(arguments);

  return rules;

})